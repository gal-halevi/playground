name: Add label
on:
  workflow_dispatch:
    inputs:
      pull_request_number:
        type: string
        required: true
jobs:
  AddLabel:
    runs-on: ubuntu-latest
    env:
      PR_NUMBER: ${{ inputs.pull_request_number }}
      GH_TOKEN: ${{ github.token }}
      REPO: ${{ github.repository }}
    steps:
      - name: Get Pull reuqest
        run: | 
          echo "ENV_PR_NUMBER=$PR_NUMBER"
          echo "INPUTS_PR_NUMBER=${{ inputs.pull_request_number }}" 
          PR=$(gh api \
          -H "Accept: application/vnd.github+json" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          /repos/$REPO/pulls/$PR_NUMBER)
          echo $PR | jq
          echo "MERGEABLE=$(echo $PR | jq '.mergeable_state')" >> "$GITHUB_ENV"

      - name: Checkout repo
        if: env.MERGEABLE == 'clean'
        uses: actions/checkout@v3
      
      - name: Create Label
        if: env.MERGEABLE == 'clean'
        run: gh pr edit $PR_NUMBER --add-label "Ready for merge"
